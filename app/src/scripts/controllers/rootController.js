/*global ADapTV, angular, $, md5 */

ADapTV.controller('RootController', ['$scope', '$rootScope', '$http', '$location', 'Storage', 'Socket',
	function rootController($scope, $rootScope, $http, $location, Storage, Socket) {
		// If user is signed in then redirect back home
		'use strict';

		
	}
	]);